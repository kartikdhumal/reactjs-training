# React Router

## 📌 Overview
This project demonstrates **client-side routing in React** using **React Router**. It includes a **multi-page application** with `Home`, `About`, and `Contact` pages, along with **dynamic routes** for a `Blog` section.

## ✨ Features
- **Multi-Page Navigation**  
  - Uses **React Router** to navigate between `Home`, `About`, and `Contact` pages.  
- **Dynamic Routing**  
  - Blog pages use dynamic routes (`/blog/:id`) to display specific blog posts.  

## 🛠️ Technologies Used
- React.js
- React Router DOM
- SCSS for styling
- Vite / Create React App (for project setup)

## ⚡ Installation & Setup
1. Clone this repository:
   ```sh
   git clone https://github.com/kartikdhumal/reactjs-training.git
   cd reactjs-training/react-router
