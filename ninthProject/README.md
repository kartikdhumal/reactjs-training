# 🛒 Shopping Cart - Redux Toolkit

## 📌 Overview
This project demonstrates **advanced state management using Redux Toolkit**. It features a **shopping cart** where users can add, update, and remove items, with the cart data being **persisted in localStorage**.

## ✨ Features
- **Add, Update & Remove Items**  
  - Users can add items to the cart.  
  - Update item **quantities**.  
  - Remove items from the cart.  
- **Total Price Calculation**  
  - The cart updates the **total price dynamically**.  
- **Persist Cart in Local Storage**  
  - Saves the cart data in `localStorage`.  
  - Restores the cart when the page is reloaded.  

## 🛠️ Technologies Used
- React.js
- Redux Toolkit for state management
- SCSS for styling
- `localStorage` for cart persistence
- Vite / Create React App (for project setup)

## ⚡ Installation & Setup
1. Clone this repository:
   ```sh
   git clone https://github.com/kartikdhumal/reactjs-training.git
   cd reactjs-training/shopping-cart-redux
