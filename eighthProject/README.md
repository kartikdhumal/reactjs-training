# Context API - Theme Switcher

## 📌 Overview
This project demonstrates **state management using React Context API**. It includes a **theme switcher** that allows users to toggle between **light and dark modes**, with the selected theme being saved in `localStorage`.

## ✨ Features
- **Theme Switching**  
  - Users can switch between **light mode** and **dark mode**.  
  - The theme is shared across all components using **React Context API**.  
- **Persist Theme in Local Storage**  
  - Saves the selected theme in **localStorage**.  
  - Restores the theme when the page is reloaded.  

## 🛠️ Technologies Used
- React.js
- Context API for global state management
- SCSS for styling
- `localStorage` for persisting theme preference
- Vite / Create React App (for project setup)

## ⚡ Installation & Setup
1. Clone this repository:
   ```sh
   git clone https://github.com/kartikdhumal/reactjs-training.git
   cd reactjs-training/context-api-theme-switcher
