# Component Lifecycle and Effects

## 📌 Overview
This project focuses on **React's component lifecycle** and **side effects using the `useEffect` hook**. It includes a **Post List App** that fetches data from an API and allows users to filter posts using a search bar.

## ✨ Features
- **Fetch Posts from API**  
  - Uses `fetch` or `axios` to retrieve posts from **JSONPlaceholder API**.  
  - Displays a loading message while fetching data.  
- **Search Functionality**  
  - Users can filter posts by title.  

## 🛠️ Technologies Used
- React.js
- SCSS for styling
- Vite / Create React App (for project setup)
- React `useEffect` for fetching data
- React `useState` for managing state

## ⚡ Installation & Setup
1. Clone this repository:
   ```sh
   git clone https://github.com/kartikdhumal/reactjs-training.git
   cd reactjs-training/component-lifecycle-effects
