# ⚛️ Custom Hooks - useFetch

## 📌 Overview
This project demonstrates how to create a **custom React hook (`useFetch`)** for fetching data from an API. The hook manages **loading and error states**, making API calls reusable and efficient.

## ✨ Features
- **Custom Hook: `useFetch`**  
  - Fetches data from any API.  
  - Handles **loading and error states**.  
- **Reusable API Calls**  
  - Easily use `useFetch` in multiple components.  
- **User List Display**  
  - Uses `useFetch` to fetch and display a list of users.

## 🛠️ Technologies Used
- React.js
- Custom Hooks (`useFetch`)
- SCSS for styling
- Fetch API / Axios for data fetching
- Vite / Create React App (for project setup)

## ⚡ Installation & Setup
1. Clone this repository:
   ```sh
   git clone https://github.com/kartikdhumal/reactjs-training.git
   cd reactjs-training/custom-hooks
