# ⚡ Performance Optimization in React

## 📌 Overview
This project demonstrates **performance optimization techniques in React** by handling a **large list of 10,000+ items** efficiently. It includes **React.memo for reducing unnecessary re-renders** and **lazy loading images** for better performance.

## ✨ Features
- **Optimized Large List Rendering**  
  - Renders **10,000+ items** efficiently using **React.memo**.  
  - Prevents unnecessary re-renders when filtering the list.  
- **Lazy Loading Images**  
  - Improves performance by loading images **only when they appear in the viewport**.  
  - Uses `loading="lazy"` for built-in browser optimization.  

## 🛠️ Technologies Used
- React.js
- React.memo for memoization
- SCSS for styling
- Lazy loading for images
- Vite / Create React App (for project setup)

## ⚡ Installation & Setup
1. Clone this repository:
   ```sh
   git clone https://github.com/kartikdhumal/reactjs-training.git
   cd reactjs-training/performance-optimization
